@echo off
REM Install dependencies manually or via package manager.
REM Example with vcpkg:
REM   vcpkg install openssl:x64-windows
REM Then configure CMake toolchain accordingly.

echo Ensure CMake, Visual Studio 2022, and OpenSSL are installed.
